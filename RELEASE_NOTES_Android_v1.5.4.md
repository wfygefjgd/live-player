# TVPlayer 安卓&TV v1.5.4

仅 Android；iOS 不变。

## 命名
- 桌面/应用名：`TVPlayer 安卓&TV`
- 安装包：`TVPlayer-安卓TV-v1.5.4-*.apk`

## 侧栏 + 遥控
| 状态 | 上/下 | 左/右 | OK | 返回 |
|------|-------|-------|-----|------|
| 侧栏关 | 换台 | 换线 | **打开侧栏** | 系统返回 |
| 侧栏开 | **只移焦点**（不换台） | 在源管理/列表间移动 | **确认选台并关栏**；焦点在源管理则打开设置 | **只关栏** |

- 点列表某台 = 播放并关栏（与 OK 一致）
- 回到全屏画面后，换台键才重新生效

## 源管理键盘
- 添加地址输入框可获焦
- 点击/获焦时 `SHOW_FORCED` 弹出软键盘（系统/电视/第三方）
- 对话框清除 `FLAG_ALT_FOCUSABLE_IM`，避免挡输入法

## 触控（不变）
- 左半屏 fling 换台/换线；右半屏不调音量亮度
- 侧栏打开时画面 fling 不换台
