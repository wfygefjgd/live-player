# TVPlayer iOS v1.5.32

## 修复你反馈的 9 点

1. **画面裁切**：`resizeAspect` 等比完整显示（不再 AspectFill 裁边）
2. **小白条**：物理全屏 + 2pt 外扩，画面层铺满安全区外
3. **默认 CCTV-1**：无上次记录时永远优先 CCTV-1；有上次关闭频道则恢复
4. **卡顿不换线**：重新接线 `onPlaybackStall`；健康检测不再把 waiting 当暂停；阈值加快
5. **设置被侧栏压住**：侧栏改独立 UIWindow；点设置先关侧栏再弹菜单
6. **功能菜单**：保留「切换来源」+「关闭」
7. **侧栏操作安心**：开栏时禁止自动/手势换台、换线、卡顿恢复
8. **滑动乱跳台**：上下台按**侧栏浏览序**（收藏→央视→…），不再用 raw 数组下标
9. **超时乱跳**：自动切台同样按浏览序下一台；选台会取消进行中的 auto-recover

## 版本
1.5.32 (66)
